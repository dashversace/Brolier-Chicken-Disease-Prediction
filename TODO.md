# TODO - Add Signup Functionality

## Tasks
- [x] Edit `server/app.py` — add users table, seed default admin, create `/api/signup` and `/api/login` endpoints
- [x] Edit `client/src/App.tsx` — add signup state/mode, `handleSignup`, update `handleLogin`, refactor `LoginScreen`
- [x] Edit `client/src/App.css` — add `.login-toggle` styles
- [ ] Restart backend and test flows


